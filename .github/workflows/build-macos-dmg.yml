name: Build macOS DMG

# Builds the native LaughCounter.app and packages it into a downloadable .dmg —
# entirely on GitHub's macOS runners, so nobody needs Xcode locally.
#
# This is the CI build: every push that touches the app produces a downloadable
# artifact. Publishing a versioned Release (with the .dmg attached) is handled by
# the separate release-macos-dmg.yml workflow, triggered by pushing a v* tag.

on:
  workflow_dispatch:        # lets you build on demand from the Actions tab
  push:
    branches: ["**"]
    paths:
      - "mac/**"
      - ".github/workflows/build-macos-dmg.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-14
    defaults:
      run:
        working-directory: mac
    steps:
      - uses: actions/checkout@v4

      - name: Swift toolchain
        run: swift --version

      - name: Build LaughCounter.app
        run: bash scripts/build-app.sh

      - name: Package .dmg
        run: bash scripts/make-dmg.sh

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: LaughCounter-dmg
          path: mac/dist/LaughCounter.dmg
          if-no-files-found: error
